class Solution(object):
    def runningSum(self, nums):
        res = [0] * len(nums)
        curr_sum = 0

        for i in range(len(nums)):
            curr_sum += nums[i]
            res[i] = curr_sum

        return res
